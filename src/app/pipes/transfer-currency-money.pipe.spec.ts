import { TransferCurrencyMoneyPipe } from './transfer-currency-money.pipe';

describe('TransferCurrencyMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new TransferCurrencyMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
